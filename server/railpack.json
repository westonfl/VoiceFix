{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "python": "3.11"
  },
  "buildAptPackages": ["ffmpeg", "libsndfile1"],
  "deploy": {
    "startCommand": "uv run uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}",
    "aptPackages": ["ffmpeg", "libsndfile1"]
  }
}
